{
  "private": true,
  "scripts": {
    "build": "eslint src && spago build --purs-args '--censor-lib --strict'",
    "test": "spago test --no-install"
  },
  "devDependencies": {
    "eslint": "^7.6.0"
  }
}
