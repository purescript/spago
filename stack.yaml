resolver: nightly-2021-04-06
packages:
- .
extra-deps:
- dhall-1.38.1
- semver-range-0.2.8
- with-utf8-1.0.2.2
nix:
  packages: [zlib]
allow-newer: true
flags:
  aeson-pretty:
    lib-only: true
  # This is so we don't depend on libtinfo
  haskeline:
    terminfo: false
    examples: false
